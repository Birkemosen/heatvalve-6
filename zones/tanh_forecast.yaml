# ============================================
# Zone with Tanh + Forecast control
# ============================================
# Same as tanh but uses forecast correction (outdoor temp, wind, illuminance).
# Include optional/forecast.yaml (and optionally bthome_shelly_blu for illuminance).
#
# Required vars: same as zones/tanh.yaml

packages:
  base: !include
    file: base.yaml
    vars:
      zone_number: ${zone_number}
      motor_number: ${motor_number}
      id: ${id}
      friendly_name: ${friendly_name}
      zone_area_default: ${zone_area_default}
      zone_max_opening_default: ${zone_max_opening_default}

  control: !include
    file: ../control/tanh_forecast.yaml
    vars:
      zone_number: ${zone_number}
      id: ${id}
      friendly_name: ${friendly_name}
      temperature_sensor: ${temperature_sensor}
      default_preset: ${default_preset}
      preset_home_temp: ${preset_home_temp}
      preset_away_temp: ${preset_away_temp}
      preset_boost_temp: ${preset_boost_temp}
      check_interval: ${check_interval}
      tanh_steepness: ${tanh_steepness}
