# =============================================================================
# HeatValve-6 \u2014 BLE Configuration (BTHome temperature sensors)
# =============================================================================
# Build:   make build
# Deploy:  make deploy
#
# Includes BLE passive scanning for BTHome temperature sensors (Shelly BLU
# H&T, etc.). HA integration via the ESPHome native API.
# Helios + custom dashboard are present.
# =============================================================================

substitutions:
  device_name: heatvalve-6
  friendly_name: "HeatValve-6 UFH Controller"

packages:
  base: !include ../heatvalve-6.yaml
  ble:  !include ../packages/hardware/ble.yaml
